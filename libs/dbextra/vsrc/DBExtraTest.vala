using aroop;
using shotodol;

/** \addtogroup fileconfig
 *  @{
 */
internal class DBExtraTest : UnitTest {
	public DBExtraTest() {
		extring tname = extring.set_static_string("DBExtra");
		base(&tname);
	}
	public override int test() throws UnitTestError {
		throw new UnitTestError.FAILED("Unimplemented\n");
		return 0;
	}
}

/* @} */
